# Backend (FastAPI)

Not yet scaffolded. Will contain the API, data models, auth logic, and AI
advisor integration.

Planned setup:
```powershell
python -m venv venv
venv\Scripts\activate
pip install fastapi uvicorn
uvicorn main:app --reload
```
